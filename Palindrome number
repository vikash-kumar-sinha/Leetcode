class Solution {
public:
    bool isPalindrome(int x) {
        long long int num=0;
        long long int ans=x;
        while(x>0){
        int r=x%10;
        num=num*10+r;
        x/=10;
        }
        if(ans==num)
        return true;
        else
         return false;

    }
};
